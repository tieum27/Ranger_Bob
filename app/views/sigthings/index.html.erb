<p id="notice"><%= notice %></p>

<h1>Sightings</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Time</th>
      <th>Region</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Animal</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sigthings.each do |sigthing| %>
      <tr>
        <td><%= sigthing.date.strftime("%d/%m/%Y") %></td>
        <td><%= sigthing.time.strftime("%H:%M:%S") %></td>
        <td><%= sigthing.region %></td>
        <td><%= sigthing.latitude %></td>
        <td><%= sigthing.longitude %></td>
        <td><%= sigthing.animal.common_name %></td>
        <td><%= link_to 'Show', sigthing %></td>
        <td><%= link_to 'Edit', edit_sigthing_path(sigthing) %></td>
        <td><%= link_to 'Destroy', sigthing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br />
<br />
<h3>Sighting Search</h3>
<form action="/sigthings" method="get">
  <label for="start_date">Start date</label>
  <input id="start_date" name="start_date" type="text">
  <label for="end_date">End date</label>
  <input id="end_date" name="end_date" type="text"><br /><br />
  <select name="region">
      <option value="Region1">Region1</option>
      <option value="Region2">Region2</option>
      <option value="Region3">Region3</option>
      <option value="Region4">Region4</option>
  </select><br /><br />
  <button>Run report</button>
</form>
<br />
<br />

<%= link_to 'New Sigthing', new_sigthing_path %>
